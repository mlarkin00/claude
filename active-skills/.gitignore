__pycache__/
*.pyc

# The counts destination is the authoring repo (mlarkin00/active-skills), which
# tracks one file per machine under skill-usage/. This is the vendored mirror,
# not that repo, so counts appearing here mean a destination was misconfigured
# to point at the marketplace checkout — keep them ignored.
.skill-usage-*.tmp
skill-usage.json
skill-usage/
