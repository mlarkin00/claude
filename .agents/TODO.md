# TODO

Marketplace-level backlog. Per-plugin backlogs live in `<plugin>/.agents/TODO.md`;
items here either span plugins or live in the workflows.

Everything below was found in the **2026-07-22 post-restructure runtime review**
(`agy` 1.1.5, Claude Code 2.1.217), which exercised both runtimes end to end.
What passed is recorded at the bottom so a re-check knows what not to re-derive.

## P0 — Address Immediately

(none — both items below closed 2026-07-22)

- [x] **[P0]** `agent-memory` and `memory-bank` install with **zero hooks** under Antigravity — both plugins are entirely hook-driven, so under `agy` they ship skills and agents but never pull/push memory or load/save context. `agy plugin install ./plugins` reports `hooks: skipped (not found)` for both. **Root cause:** a root `plugin.json` puts `agy` on its native path, where it looks for a root `hooks.json`; their hooks live in `hooks/hooks.json` (Claude format), which `agy` only reads on its claude-format path. Proven by A/B bulk-install with the root manifest removed — both then report `✔ hooks : 1 processed`. Both had working root `hooks.json` files in the retired `agy-plugins` repo; the consolidation into this repo dropped them. **Fix:** hand-write a root `hooks.json` for each, following the one pattern known to work here — `skill-usage/hooks.json`: relative `./scripts/…` commands, no `$CLAUDE_PLUGIN_ROOT` (see P0 below), and only events `agy` supports. *(2026-07-22: fixed. Added `agent-memory/hooks.json` and `memory-bank/hooks.json`; both now report `✔ hooks : 2 processed` where they reported `skipped (not found)`.*

  *Corrects two claims made when this was filed. **`agy` has no `SessionStart`.** The binary embeds the full spec — the supported set is exactly `PreToolUse`, `PostToolUse`, `PreInvocation`, `PostInvocation`, `Stop`; the earlier list came from a raw string match, not the schema. `PreToolUse`/`PostToolUse` take a `{matcher, hooks}` wrapper, the other three a flat handler list. And the **`llm-wiki` trap is void** — see the item below.*

  *The `SessionStart` → `PreInvocation` mapping needed more than a rename: `PreInvocation` fires before **every** model call and blocks the agent loop, so the once-per-session work had to be gated or it would run per turn (`load_context.py` costs a measured ~3.6s Vertex round-trip; `memory-pull.sh` a `git fetch`). Both plugins gained a wrapper keyed on `conversationId` — `agent-memory/scripts/agy-session-start.sh`, `memory-bank/scripts/agy_load_context.py` — with state under `$XDG_CACHE_HOME`, self-pruning after 7 days.*

  ***The two wrappers are not the same shape, and live testing is what proved it.** `agent-memory`'s work (a `git fetch`) is genuinely once-per-session, so it simply skips after the first invocation. `memory-bank`'s is not: `PreInvocation`'s `ephemeralMessage` is **transient**, living only for the invocation it was injected into. A first cut that skipped after turn one passed every offline test and then failed in a real session — turn 2 of the same conversation answered "NO MEMORIES INJECTED". So `agy_load_context.py` now **caches and replays**: it pays the Vertex round-trip once per conversation, stores the rendered payload, and re-emits it on every later invocation, refreshing after 30 minutes so a long conversation still picks up newly added memories. Measured 3.98s on the first turn, 0.03s after, with memories present on both. Only a live multi-turn session surfaces this — nothing offline distinguishes "gated correctly" from "memories silently gone".*

  *Deliberately **not** carried over to Antigravity: `install-symlinks.sh`. It links into `~/.claude/`, so running it from the Antigravity copy would repoint Claude Code's symlinks at the Antigravity plugin directory and the two installs would fight over the same links. Nothing in the agy hooks needs it — their commands are relative to the plugin root, which is the working directory `agy` guarantees. This does leave `agent-memory` and `memory-bank` **skills** calling `~/.claude/scripts/...` paths that only exist if Claude Code also installed them; filed as its own P2 below.)*

- [x] **[P0]** Hooks converted by `agy` from Claude format resolve to an empty plugin root — `$CLAUDE_PLUGIN_ROOT` **does not exist in the Antigravity runtime**. The string does not occur anywhere in the 188 MB `agy` binary, and no equivalent (`*_PLUGIN_ROOT`, `AGY_*`) is defined. `llm-wiki` takes the claude-format path today, and `agy` copied `hooks/hooks.json` verbatim into a generated root `hooks.json`, `$CLAUDE_PLUGIN_ROOT` and all; running that command with the variable unset gives `bash: /hooks/validate-on-write.sh: No such file or directory`. So `llm-wiki`'s `PostToolUse` OKF validator is dead under `agy` **despite the installer reporting `✔ hooks : 1 processed`** — the count is not evidence the hook works. **Fix:** any hook intended to run under `agy` must use a path relative to the plugin directory, which means a hand-written root `hooks.json` rather than relying on conversion. *(2026-07-22: fixed, and the "trap" recorded in the item above turned out to be wrong.*

  *Two behaviours settled by experiment. **A hand-written root `hooks.json` is merged, not replaced** — install `llm-wiki` with one and `agy` keeps the named hook **and** appends its own converted block under the key `"hooks"`, so both fire and the broken converted copy still runs on every tool call. **A root `plugin.json` puts the plugin on the native path**, which skips `hooks/hooks.json` entirely. The earlier "adding a manifest means hooks are never installed" was inferred from `agent-memory`/`memory-bank`, which simply have no `commands/` to convert.*

  ***Correction (2026-07-22, from live testing).** This item first claimed a root `plugin.json` does **not** cost the command conversion. That was wrong — it trusted the installer's output instead of checking disk. A clean A/B: without the root manifest `agy` writes 10 `llm-wiki-cmd-*` skill directories; with it, **zero**. Both runs print the identical line `commands : 10 processed (converted to skills)`, so that line is not evidence of anything (see the P2 below). Adding `llm-wiki/plugin.json` therefore did drop the converted commands under Antigravity. Keeping it is still correct: those 10 commands are thin wrappers duplicating 10 identically-named skills, `agy` now exposes the 17 real skills instead, and the `stats` skill was confirmed loading from the installed plugin in a live session. The alternative — dropping the manifest to regain them — reinstates the merged broken hook firing on every tool call.*

  *So `llm-wiki` got both files: `hooks.json` (relative command) and `plugin.json` (agy manifest, 0.1.1, matching the Claude manifest). A fresh bulk install now registers exactly one hook per plugin and no `$CLAUDE_PLUGIN_ROOT` leftovers: `agent-memory` → `[agent-memory-pull, agent-memory-push]`, `llm-wiki` → `[llm-wiki-okf-validate]`, `memory-bank` → `[memory-bank-loader, memory-bank-writer]`, `skill-usage` → `[skill-usage]`.*

  *Path resolution alone would have left the hook running but blind, because `validate-on-write.sh` also parsed only Claude's payload shape. It now reads both (`tool_input.file_path|path`, and `toolCall.args.AbsolutePath|TargetFile|…`), resolves the validator from its own location via `readlink -f` instead of `$CLAUDE_PLUGIN_ROOT`, and answers each runtime in its own contract: Claude keeps the blocking non-zero exit, Antigravity gets `{}` on stdout with the violation on stderr, since its `PostToolUse` reply cannot block or reach the model. Verified against a fixture bundle — a real OKF §9 violation now reports on both runtimes, and Claude's blocking exit survives `$CLAUDE_PLUGIN_ROOT` being unset, which it did not before.)*

## P1 — Important / Unblocking

- [ ] **[P1]** Neither sidecar is registered or run by Antigravity — both are dead files as shipped. `agy` loads sidecars from **`<config>/sidecars/<id>/sidecar.json`** (i.e. `~/.gemini/config/sidecars/`), one directory per sidecar, per the spec embedded in the binary; it does not look inside `plugins/<name>/sidecars/`, which is where `agy plugin install` puts ours. Confirmed against a live install: `~/.gemini/config/sidecars/` does not exist, no `sidecar_data/` runtime directory was ever created, no sidecar process is running, and the installer never lists sidecars as a component at all. The visible consequence: `skill-usage` counted a real skill invocation in a live session (`systematic-debugging` -> 1) but the counts file stays **uncommitted**, because committing and pushing is `sidecars/sync-usage`'s job. `active-skills`' `check-updates` never runs either — academic while it polls a 404 (below), but broken independently. **Fix:** install sidecars into `~/.gemini/config/sidecars/<id>/` (symlink or copy from the plugin — the execution CWD is the sidecar's own folder, which `check_updates.py` already assumes), or drop them and move the work into a hook. A plugin cannot ship a working sidecar by placing it under `sidecars/` and expecting `agy plugin install` to find it.

- [ ] **[P1]** Plugin agents install but cannot be invoked under Antigravity — all eight (`agent-memory` 3, `memory-bank` 1, `llm-wiki` 4) are copied to `plugins/<name>/agents/` and counted at install (`agents : 3 processed`), but `agy agents` lists nothing and a live session asked directly answered **"NO PLUGIN SUBAGENTS"**, offering only the built-in `research` and `self`. They are not converted to skills either. The agent layer of three plugins is therefore inert on that runtime: anything that says "run the bootstrap-memory agent" — including `verify-memory.sh`'s own remediation message — has no way to happen there. **Fix:** decide whether these become skills for Antigravity (the only component type that demonstrably loads) or whether the plugins document agents as Claude-only. Pair with the `agent-memory` agent-frontmatter item below, since fixing that is wasted effort if the agents are unreachable anyway.

- [ ] **[P1]** `agent-memory`'s three agents load with **empty metadata** — `agents/bootstrap-memory.md`, `memory-puller.md`, `memory-pusher.md` all fail YAML frontmatter parsing: the `<example>` blocks sit *inside* the `---` fences, after the `description:` string has closed, so the parser hits raw XML at the top level of the mapping (`while scanning a simple key`). Flagged by `claude plugin validate agent-memory` and confirmed independently with PyYAML. **Confirmed live**, not theoretical — the session agent roster shows `agent-memory:bootstrap-memory: Agent from agent-memory plugin (Tools: All tools)` (placeholder description, no tool restriction) against a correctly-parsed sibling, `memory-bank:bootstrap-memory-bank: Use when setting up… (Tools: Bash, Read, Edit)`. Dropped on all three: the `description` (so the model cannot tell when to invoke them), `tools: [Bash]` (**they run with every tool instead**), and `model: haiku` (they run on the session model). **Fix:** move the `<example>` blocks below the closing `---`, matching `memory-bank/agents/bootstrap-memory-bank.md` and `llm-wiki/agents/*.md`. Re-verify with `claude plugin validate agent-memory` **and** by confirming the roster shows the real description and `(Tools: Bash)`.

- [x] **[P1]** `release.yml` never bumps the Antigravity manifests — the bump step writes only `$p/.claude-plugin/plugin.json` and the `marketplace.json` entry, so every plugin it owns has drifted: `agent-memory` 0.3.3 vs 0.1.0, `memory-bank` 0.1.17 vs 0.1.0, `skill-usage` 0.1.2 vs 0.1.0. Only `sync-active-skills.yml` writes both, which is why `active-skills` is the one plugin in agreement (0.2.7/0.2.7). The stated convention in `AGENTS.md` ("both manifests carry the **same** version — one plugin serves both runtimes") is therefore true of exactly one plugin. **Fix:** loop the bump over both manifest paths in `release.yml`. As of 2026-07-22 every plugin has a root `plugin.json` — `llm-wiki` gained one with the P0 hooks fix — so no skip case is needed, but guard for a missing file anyway rather than letting `jq` abort the step under `set -euo pipefail`. Restamp the three drifted manifests in the same commit — but note that hand-editing a version for a plugin in `PLUGINS` races the bot, so do it in a commit the bot will not also act on, or let the next release carry it. *(2026-07-22: fixed. The bump loop now writes both manifests, guarded with `[ -f ]` so a plugin without an Antigravity manifest logs a `::notice::` instead of aborting the step — a bare `jq` read of a missing file would kill the release under `set -euo pipefail`. Dry-run against a copy of the repo confirmed all three versions agree afterwards for plugins with both manifests, and that the missing-manifest case exits 0.*

  *The four drifted Antigravity manifests were restamped to their current Claude versions in the same commit. That is not the hand-bump this item warned against: it does not touch the number the bot reads (the Claude manifest) or the marketplace entry, so there is no race — it makes those files release-relevant, and the bot's next run bumps all three in lockstep from the Claude version.)*

- [x] **[P1]** The `AGENTS.md` verification snippet cannot catch the drift above — it compares `marketplace.json` against `.claude-plugin/plugin.json` only, so all five plugins report `OK` while three Antigravity manifests are two minor versions stale. **Fix:** extend it to read the root `plugin.json` where present and flag disagreement; pair with the `release.yml` fix so the check and the writer land together. This is the check that was supposed to make manifest/disk disagreement impossible, so leaving it half-blind is how the next drift goes unnoticed. *(2026-07-22: fixed alongside the writer, as this item asked. The snippet now prints `mkt=`/`claude=`/`agy=` per plugin and marks a row BAD unless all three agree, treating a missing Antigravity manifest as acceptable. The versioning rule above it was also wrong — it named two places when there are three — and now says so.)*

- [x] **[P1]** `memory-bank`'s session-end save reads a key Claude Code never sends — `save_context.py:58` takes the transcript from `input_data.get('transcriptPath')`, which is Antigravity's protojson camelCase. Claude Code hook payloads are snake_case (`transcript_path`, `session_id`, `tool_name`), as the working Claude-side code in this repo shows (`skill-usage/scripts/track-usage.py` reads `tool_name`/`tool_input`). With no `transcriptPath` the lookup returns `None` and the function returns at the `not transcript_path` guard, so **the Stop hook has been a silent no-op under Claude Code** — it runs, exits 0, and sends nothing to Memory Bank. Found while wiring the Antigravity hooks, where the same script is correct: `transcriptPath` is exactly what `agy` sends. The test suite does not catch it because `test_save_context.py` feeds `transcriptPath` in all four cases, so it only ever exercises the Antigravity shape. **Fix:** accept both keys (`input_data.get('transcriptPath') or input_data.get('transcript_path')`) and add a Claude-shaped case to the tests. Check `workspacePaths` vs `cwd` in the same pass — the same mismatch applies to the scope resolution on line 59. Not fixed with the P0 because it is a Claude-side defect in a different script, and it needs test changes rather than a hooks file. *(2026-07-22: fixed. `save_context.py` now reads `transcriptPath` or `transcript_path`, camelCase first so a runtime sending both is unaffected. Proven end to end: a real Claude Stop payload against this session's own 1324-line transcript now reaches the API layer with 116 events at global scope, where before it returned at the guard with none.*

  *Three test-suite problems surfaced while adding coverage. The four existing cases all fed Antigravity keys, so the suite was green against a hook that never fired on Claude. Worse, every case that called `sys.modules.pop('save_context')` **inside** the test body discarded the patched module, so the fresh import rebound the real `send_generation_request` and the tests were making live Vertex AI calls — which also made `test_missing_transcript_does_not_call_api` pass vacuously, since an unpatched mock can never register a call. Removing the in-test pops (tearDown already handles it) cut the suite from 13.2s to 2.9s. The new Claude-shaped case was verified to FAIL against the pre-fix code, so it genuinely guards the regression.*

  *The `workspacePaths` vs `cwd` half of this item was a false alarm: `workspace_path` was assigned and never used, `resolve_project_id` imported and never called, and `project_hash` hardcoded to `"global"`. The dead lines and unused import are removed and the intent documented — session-end consolidation is global on purpose.)*

- [ ] **[P1]** `llm-wiki` registers **10 duplicate skill names** in Claude Code — `claude plugin details llm-wiki` reports `Skills (27)` with `enrich, index, ingest, init, lint, log, query, stats, validate, visualize` each listed twice, because the plugin ships both `commands/<n>.md` and `skills/<n>/SKILL.md` for all ten. Claude Code now surfaces commands as skills, so the two collide and **the command wins**: the session skill roster shows the *command's* description for `llm-wiki:stats` and `llm-wiki:enrich`, meaning the `SKILL.md` bodies — the actual instructions — never load. The command bodies then say things like "Load the `stats` skill for interpretation", a hop that cannot resolve because the name routes back to the command. Costs ~700 always-on tokens for the shadowed copies. Antigravity no longer has this problem, though not by design: since `llm-wiki` gained a root `plugin.json` with the P0 fix, `agy` takes the native path and does not convert the commands at all, so only the 17 real skills load there. **Fix:** pick one surface per name. Either delete the ten thin `commands/*.md` wrappers and let the skills carry `/llm-wiki:<n>` invocation, or keep the commands as the entry point and fold the skill bodies into them. Deleting the commands is the smaller change and preserves the richer content.

- [ ] **[P1]** `active-skills`' update-check sidecar polls a URL that is now a permanent 404 — `sidecars/check-updates/check_updates.py:62` fetches `https://raw.githubusercontent.com/mlarkin00/active-skills/main/plugin.json`, but the source repo went skills-only in the 2026-07-22 restructure and has no manifest (verified HTTP 404; the repo root holds 39 skill dirs plus `AGENTS.md` and `README.md`). It fails soft, so it will log `Update check failed. Preserving last known state` forever; its cached state already reads `local=0.1.4, remote=0.1.5` against an actual 0.2.7. **Fix:** point it at this repo's manifest (`raw.githubusercontent.com/mlarkin00/plugins/main/active-skills/plugin.json`), since the version now lives here — or delete the sidecar if nothing consumes its output. Note `tests/test_check_updates.py` passes today because it mocks the fetch, so the suite will not catch the next URL rot; assert the URL resolves, or at least that it targets this repo.

## P2 — Nice-to-Have

- [ ] **[P2]** Do not trust `agy plugin install`'s component counts as evidence anything works — three of them proved false during this review. It prints `commands : 10 processed (converted to skills)` identically whether it writes 10 skill directories or zero; it printed `hooks : 1 processed` for a hook whose command resolved to a nonexistent path; and it counts `agents : 3 processed` for agents no session can invoke. The counts report what the installer walked, not what the runtime can use. **Practical rule for this repo:** verify a component by observing an effect — a marker file, a counted skill, a validator message in `~/.gemini/antigravity-cli/cli.log`, a committed memory — never by the installer's summary. Worth a line in `AGENTS.md` next to the other verification commands.

- [ ] **[P2]** `agent-memory` and `memory-bank` skills call `~/.claude/scripts/…` paths that do not exist on an Antigravity-only machine — e.g. `memory-bank/skills/memories-add/SKILL.md` runs `python3 ~/.claude/scripts/memory-bank/add_memory.py`, and `agent-memory/skills/verify-memory/SKILL.md` runs `bash ~/.claude/scripts/verify-memory.sh`. Those links are created by `install-symlinks.sh`, which the Antigravity hooks deliberately do not run (see the P0 above — running it from the agy copy would hijack Claude Code's links). So under Antigravity these skills instruct the agent to execute a path that is absent unless Claude Code happens to be installed too. **Fix:** have the skills resolve the script relative to the plugin they ship in rather than a fixed user-level path, or fall back to the plugin copy when the symlink is missing. Worth doing together with a decision on whether `install-symlinks.sh` should exist at all — the indirection is also what let a stray run repoint Claude's links at a dev checkout during the 2026-07-22 review.

- [ ] **[P2]** Quote the `description` in `skills/skill-portfolio-review/SKILL.md` — the unquoted scalar contains `report: `, so a strict YAML parser aborts with `mapping values are not allowed here` (flagged by `claude plugin validate`, reproduced with PyYAML). Claude Code's runtime reader is lenient enough that the description currently loads, so this is latent rather than broken — but the validator's verdict is "loads with empty metadata", and a parser change would make that true. **Must be fixed upstream in `mlarkin00/active-skills`** — `active-skills/skills/` here is the rsync mirror and any local edit is overwritten on the next sync.

- [ ] **[P2]** Quote the `description` in `llm-wiki/commands/stats.md` — same failure mode, the scalar contains `bundle: `. Moot if the duplicate-commands item above resolves by deleting the wrappers; fix whichever file survives.

- [ ] **[P2]** `active-skills/README.md` contradicts the current design — it still says "The repository root *is* the plugin", "The two manifests coexist and **each carries its own version**", and describes a release workflow in the source repo that bumps "only Antigravity" for a `sidecars/` change, closing with "Don't hand-edit a `version`; the bot owns it". All four are false as of `0ca72e7`/`8643ac6`: both manifests carry one version, the source repo has no release workflow (its own README is correct and says so), and `AGENTS.md` now *requires* a hand bump for changes outside `skills/`. This file is hand-owned here (only the block between the `SKILLS:START/END` markers is generated), so it is this repo's to fix. Also reconsider the framing — the vendored copy still addresses the reader as if they were in the authoring repo ("clone it to author skills").

- [ ] **[P2]** `agent-memory/README.md` ships stale install instructions — line 34 says `claude plugin marketplace add mlarkin00/agent-memory-plugin`, a repo that was renamed to `mlarkin00/plugins` and now works only via GitHub's redirect, and the next step's `claude plugin install agent-memory` omits the `@mlarkin00-plugins` marketplace qualifier. Also worth reviewing the "GitHub repo ([agent-memory](https://github.com/mlarkin00/agent-memory))" links, which point at a private repo and 404 for anyone else.

- [ ] **[P2]** `memory-bank` carries its GCP `config` block in **both** manifests, but only `.claude-plugin/plugin.json` is ever read — `scripts/config.py` resolves `../.claude-plugin/plugin.json` explicitly, so the copy in the root `plugin.json` is dead weight on both runtimes (under `agy` the plugin directory still contains the Claude manifest, which is why config resolution works there at all). The two agree today; nothing keeps them in step. Drop the duplicate, or have `config.py` prefer the root manifest and fall back.

- [ ] **[P2]** Note in `AGENTS.md` that `agy plugin validate <path>` is not a usable per-plugin check for this repo — it hard-fails with `missing plugin.json` on any plugin without a root manifest (`llm-wiki` today), because single-path validation takes the native path only. The bulk form (`agy plugin install <clone>`) is what exercises the claude-format conversion, so that is the command to reach for when verifying a change. Worth capturing next to the existing manifest-agreement snippet.

- [ ] **[P2]** *(operational, not a repo defect)* The dev machine's live Antigravity install holds only `active-skills`, skills component only — `~/.gemini/config/plugins/` has one entry and `import_manifest.json` lists one import, dated 2026-07-22T18:28Z. The other four plugins are not registered, so nothing local is exercising them under `agy`. Re-run `agy plugin install ~/plugins` after the P0 hook items land, so the fix is verified against a real install rather than a sandbox.

## Verified working — 2026-07-22

Do not re-derive these; re-check only if the relevant machinery changes.

- Claude marketplace: all 5 plugins installed and enabled; `marketplace.json`, `.claude-plugin/plugin.json`, and the installed versions agree; cached content byte-identical to the repo (`skill-usage/README.md` differs by a doc-only change `release.yml` correctly declined to release).
- `agy plugin install ./plugins` → `Found bulk plugins directory`, all 5 install, exit 0.

### Antigravity, exercised in live sessions — 2026-07-22

Observed effects, not installer output. `agy` 1.1.5, real `agy -p` runs.

- **memory-bank load** — a session answered the documentation-lookup question verbatim from Memory Bank; a second turn in the same conversation still had them after the cache-and-replay fix (it did not before). `memories-list` skill ran its script and returned 12 real memories.
- **memory-bank Stop** — plugin-registered `Stop` hooks fire in print mode, proven with a temporary probe hook added to the installed copy and removed after. `sidecar_consolidate.py` correctly self-throttled on its 24h gate.
- **agent-memory** — both `PreInvocation` and `PostToolUse` fire: the conversation marker appeared under `~/.cache/agent-memory/agy-conversations/` with the session's real `conversationId`, and the push hook committed and pushed a test memory file (`memory: zz-agy-hook-test.md`, local HEAD == origin/main). Test file removed in a follow-up commit. `verify-memory` skill ran clean at exit 0.
- **active-skills** — a session invoked `systematic-debugging` and returned its four phases verbatim.
- **skill-usage** — the same invocation incremented `systematic-debugging` to 1 in the counts file. It is **not** committed, because that is the sidecar's job (see P1).
- **llm-wiki** — `okf_validate.py`, `okf_stats.py` and `okf_index.py` all run from the installed copy; a live session wrote an OKF-violating doc and the `PostToolUse` validator reported `frontmatter 'type' is missing or empty (required by OKF §9)` into `~/.gemini/antigravity-cli/cli.log`. The `stats` skill loads and cites `okf_stats.py`.
- **Not working there:** plugin agents (unreachable) and both sidecars (never registered) — filed as P1.
- `sync-active-skills.yml`'s skill-selection filter, replayed against the live source repo: 39 skills selected, nothing skipped, no `README.md`/`AGENTS.md`/`.git` leakage, result byte-identical to the current mirror.
- `active-skills/scripts/gen-readme.sh` is idempotent (39 skills, no diff).
- Test suites green: `memory-bank` 25/25, `active-skills` 3/3 (`python3 -m unittest discover -s tests`; pytest is not installed on this machine).
- Every `.py` in the repo compiles; every `.sh` parses.
- Hook smoke tests: `memory-bank/scripts/load_context.py` returns memories; `agent-memory`'s `install-symlinks.sh` and `verify-memory.sh` exit 0; `skill-usage`'s trackers count correctly on both runtimes' payload shapes.
- No unlisted plugin directories on disk.
