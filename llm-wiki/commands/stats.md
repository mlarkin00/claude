---
description: Show quick mechanical stats for an OKF bundle: concept count by type, total links, orphans (no inbound links), broken links (target doesn't exist), and citation coverage.
---

Run mechanical bundle stats.

```bash
python3 "$CLAUDE_PLUGIN_ROOT/scripts/okf_stats.py" <bundle_root>
```

Load the `stats` skill for interpretation and next-step suggestions.
