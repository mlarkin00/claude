---
description: Initialize a new OKF v0.1 knowledge bundle. Creates index.md (with okf_version frontmatter), CLAUDE.md (per-bundle schema file), .gitignore, and optional raw/ directory.
---

Scaffold a new OKF bundle at the given path (defaults to current directory).

Load the `init` skill for the full implementation.
